<header class="navbar navbar-fixed-top navbar-inverse">
    <div class= "container">
      <ul class="nav navbar-nav navbar-right">
        <li id="home"><%= link_to "Home", root_path %></li>
        <li id="about"><%= link_to "About", about_path %></li>
        <% if logged_in? %>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown">
            Account <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li id="edit"><%= link_to "Update Profile", edit_user_path(current_user) %></li>
            <li id="logout"><%= link_to "Logout", logout_path, method: :delete %></li> 
          </ul>
        </li>        
        <% else %>
        <li id="login"><%= link_to "Login", login_path %></li>       
        <% end %> 
      </ul>
    </div>
</header>